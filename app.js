const input=document.getElementById('search');const clear=document.getElementById('clear');function filter(){const q=input.value.trim().toLowerCase();document.querySelectorAll('.card').forEach(c=>{c.classList.toggle('hidden',q && !c.dataset.search.includes(q));});document.querySelectorAll('section').forEach(s=>{const visible=s.querySelectorAll('.card:not(.hidden)').length;s.classList.toggle('hidden',q && visible===0);});}input.addEventListener('input',filter);clear.addEventListener('click',()=>{input.value='';filter();input.focus();});